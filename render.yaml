services:
  - type: web
    name: highest-data-hub
    runtime: node
    region: oregon
    buildCommand: npm install && npm --prefix backend install && npm --prefix backend run build
    startCommand: NODE_ENV=production npm start
    envVars: []
